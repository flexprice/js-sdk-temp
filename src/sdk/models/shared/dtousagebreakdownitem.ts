/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DtoUsageBreakdownItem = {
  /**
   * cost is the cost attributed to this group for the line item
   */
  cost?: string | undefined;
  /**
   * event_count is the number of events from this group (optional)
   */
  eventCount?: number | undefined;
  /**
   * grouped_by contains the grouping field values (e.g., {"source": "api", "org_id": "org123"})
   */
  groupedBy?: { [k: string]: string } | undefined;
  /**
   * percentage is the percentage of total line item cost from this group (optional)
   */
  percentage?: string | undefined;
  /**
   * usage is the total usage amount from this group (optional, for additional context)
   */
  usage?: string | undefined;
};

/** @internal */
export const DtoUsageBreakdownItem$inboundSchema: z.ZodType<
  DtoUsageBreakdownItem,
  z.ZodTypeDef,
  unknown
> = z.object({
  cost: z.string().optional(),
  event_count: z.number().int().optional(),
  grouped_by: z.record(z.string()).optional(),
  percentage: z.string().optional(),
  usage: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "event_count": "eventCount",
    "grouped_by": "groupedBy",
  });
});

export function dtoUsageBreakdownItemFromJSON(
  jsonString: string,
): SafeParseResult<DtoUsageBreakdownItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DtoUsageBreakdownItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DtoUsageBreakdownItem' from JSON`,
  );
}
